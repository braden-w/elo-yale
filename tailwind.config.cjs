const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			sans: ['Habibi', 'sans-serif'],
			serif: ['Habibi', 'serif']
		},
		extend: {}
	},

	plugins: []
};

module.exports = config;
