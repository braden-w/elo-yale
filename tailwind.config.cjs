const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			sans: ['habibi', 'sans-serif'],
			serif: ['habibi', 'serif']
		},
		extend: {
			fontFamily: {
				habibi: ['Habibi']
			}
		}
	},

	plugins: []
};

module.exports = config;
